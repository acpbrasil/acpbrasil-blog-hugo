;(function () {
	'use strict';

	// Document on load.
	$(function() {
		
		/* Disable the window.scroll event handler - the navbar must stay fixed at the top. */
		// windowScroll();
		$(window).off('scroll');

	});

}());